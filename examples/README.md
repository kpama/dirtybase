# Examples

This folder contains numerous examples for most of the crates in this workspace.
Examples are prefixed with the crate's name. Example: `app-` indicates that the example is for the `dirtybase_app` crate